package mursalin.companion.gobuddy.data.local.dao

import mursalin.companion.gobuddy.domain.model.Project

class ProjectDao {
    fun insert(project: Project) {
        // Insert the project into the database
    }
}